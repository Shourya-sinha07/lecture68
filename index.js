const average= require("./mod");
console.log(average([5,5]))
console.log("this is index.js")
//from mod.js we export only the average function so we have access that function by name only that average[5,5] (name can we anything the condition is variable which store the require value and the funtion is used must be same )and we pass a object then:-
// const mod= require("./mod");
// console.log(mod.average([5,5]))
// console.log(mod.name)
// console.log(mod.repo)
